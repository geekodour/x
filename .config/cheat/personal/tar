# extract:
tar -xvf /path/to/foo.tar # uncompressed
tar -zxvf /path/to/foo.{tgz|tar.gz}
tar -jxvf /path/to/foo.tar.bz2
tar -xvf /path/to/foo.tar -C /path/to/destination/ # extract to

# create:
tar -cvf /path/to/foo.tar /path/to/foo/ # uncompressed
tar -zcvf /path/to/foo.{tgz|tar.gz} /path/to/foo/
tar -jcvf /path/to/foo.tar.bz2 /path/to/foo/

# list:
tar -tvf /path/to/foo.tar # uncompressed
tar -ztvf /path/to/foo.{tgz|tar.gz}
tar -jtvf /path/to/foo.tar.bz2
