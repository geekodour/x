# some basic strokes after (Ctrl+a):
| key    | description      |
| -------| -----------------|
| d      | detach session   |
| t      | big clock        |
| ?      | list shortcut    |
| :      | prompt           |
| w      | list windows     |
| s      | list sessions    |
| $      | rename session   |
| x      | kill pane/window |
| |      | v split          |
| -      | h split          |
| [0-9]  | switch to window |

# detach already attached session
# (moving devices w diff resolutions):
tmux attach -d 

# attach tmux to session
tmux attach -t <session_select>

# start tmux session with name
tmux new -s <session_name>

# To display session:
tmux ls
